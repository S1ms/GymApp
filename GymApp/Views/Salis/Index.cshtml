@model IEnumerable<GymApp.Models.Sali>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Tallenna salikäynti</h1>

<p>
    <a asp-action="Create">Luo uusi</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reps)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sets)
            </th>


        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {

            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Reps)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sets)
                </td>


                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Muokkaa</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Tiedot</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Poista</a>
                </td>
            </tr>
        }

    </tbody>
</table>
